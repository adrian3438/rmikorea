# Copy to scripts/deploy.env and fill in the real target.
# deploy.env is gitignored — the values below must not be committed.

RMI_HOST=user@your-server-ip
RMI_SSH_KEY=$HOME/.ssh/your_deploy_key

# Optional overrides:
# RMI_REMOTE_ROOT=/var/www/rmikorea
# RMI_SITE=https://rmikorea.com/
